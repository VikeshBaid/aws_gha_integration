name: Adding SSH Key
on:
  workflow_dispatch:


jobs:
  Deploy-EC2-using-GHA:
    name: 'CS sensors upload'
    runs-on: ubuntu-latest

    steps:
      - name: checkout repo contents
        uses: actions/checkout@v3

      #- name: configure aws credentials
      #  uses: aws-actions/configure-aws-credentials@v1
      #  with:
      #    role-to-assume: arn:aws:iam::792514764471:role/configroleawsgha
      #    role-session-name: samplerolesession
      #    aws-region: us-east-1

      - name: Create SSH key
        run: |
          mkdir -p ~/.ssh/
          echo "$SSH_PRIVATE_KEY" > ../private.key
          sudo chmod 600 ../private.key
          echo ${{ github.workspace }}
        shell: bash
        env:
          SSH_PRIVATE_KEY: ${{secrets.SSH_PRIVATE_KEY}}
          SSH_KEY_PATH: ${{ github.workspace }}/../private.key

      - name: upload file to github
        run: |
          git clone git@github.com:VikeshBaid/aws_gha_integration.git
